<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ThoughtBox</title>
    <body background="http://215marketinghouse.com/wp-content/uploads/2015/05/Simple-Grey-Background.jpg">
  </head>
  <body>
    <h2>Welcome to ThoughBox</h2>
    <p>Please Login on Sign up</p>
    <a class="btn-large waves-effect waves-light blue" id="log-in">Login</a>
    <a class="btn-large waves-effect waves-light green" id="sign-up">Sign Up</a>
    <div class="login-form">
      <%= form_for(:session, url: login_path) do |f| %>
        <%= f.label :email %>
        <%= f.text_field :email %>
        <%= f.label :password %>
        <%= f.password_field :password %>
        <%= f.submit "Login", class:"btn" %>
      <% end %>
    </div>
    <div class="signup-form">
      <%= form_for(:user, url: users_create_path) do |f| %>
        <%= f.label :email %>
        <%= f.text_field :email %>
        <%= f.label :password %>
        <%= f.password_field :password %>
        <%= f.label :confirm_password %>
        <%= f.password_field :password %>
        <%= f.submit "Sign Up", class:"btn" %>
      <% end %>
    </div>
  </body>
</html>
